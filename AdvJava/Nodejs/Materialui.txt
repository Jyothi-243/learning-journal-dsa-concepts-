Material UI : 
    Typography(text-content) : used for displaying the text content(instead of using paragraph and heading). Typography takes so many props (varient= "h1") // <typography varient= "h4" >
    for the input fields in the form ,we have to use text tags in material UI. 
    <AppBar> </AppBar> component  and <Toolbar> </Toolbar>  , 
    <Container> </Container> 
    <Typography paragraph>
1.we can use inline- styling also in the material-ui components. 
<Container style = {{"marginTop":"70px", "TextAlign":"center"}}> </Container>  

we can use <ImageList rowHeight={170} cols = {3}> <ImageListItem></ImageListItem></ImageList>  
// we can use rowHeight to mention height of the row. 
<img src = {} alt = {}/> //pixabay website. 